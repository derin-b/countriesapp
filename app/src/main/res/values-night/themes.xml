<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CountryDetail" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:drawableTint" tools:targetApi="m">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark_background</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/dark_background</item>
    </style>

    <style name="editTextView" parent="ThemeOverlay.Material3.TextInputEditText.FilledBox">
        <item name="android:background">@color/dark_search</item>
        <item name="android:fontFamily">@font/axiforma_book</item>
    </style>

    <style name="toolText" parent="Widget.MaterialComponents.TextView">
        <item name="android:textSize">36sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="fontWeight">700</item>
        <item name="fontFamily">@font/parkside_bold</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="imageButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:src">@drawable/moon</item>
        <item name="android:background">@drawable/image_btn_night</item>
    </style>
</resources>